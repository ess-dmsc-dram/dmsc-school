# Book settings
# Learn more at https://jupyterbook.org/customize/config.html

title: ESS Data Management and Software Centre Summer School
author: Data Management and Software Centre
# logo: logo.png
copyright: "2026"

parse:
  myst_enable_extensions:
    # - amsmath
    - colon_fence
    # - deflist
    - dollarmath
    # - html_admonition
    - html_image
    - linkify
    # - replacements
    # - smartquotes
    - substitution
    - tasklist

# See https://jupyterbook.org/content/execute.html
execute:
  timeout: -1
  execute_notebooks: cache
  allow_errors: false
  stderr_output: "error"
  exclude_patterns:
    - '3-mcstas/Talk.ipynb'
    - '5-analysis/analysis-qens.ipynb'  # TODO: remove once new QENS analysis is ready
    - '6-scicat/9-exercise.ipynb'
    - '6-scicat/workbooks/*'

# Define the name of the latex output file for PDF builds
latex:
  latex_documents:
    targetname: book.tex

# Add a bibtex file so that we can create citations
bibtex_bibfiles:
  - references.bib

# Information about where the book exists on the web
repository:
  url: https://github.com/ess-dmsc-dram/dmsc-school  # Online location of your book
  path_to_book: docs  # Optional path to your book, relative to the repository root
  branch: main  # Which branch of the repository should be used when creating links (optional)

# Add GitHub buttons to your book
# See https://jupyterbook.org/customize/config.html#add-a-link-to-your-repository
html:
  favicon: "images/favicon.ico"  # A path to a favicon image
  use_issues_button: true
  use_repository_button: true

sphinx:
  extra_extensions:
    - sphinxcontrib.mermaid
  config:
    nb_execution_show_tb: True
    suppress_warnings:
      # From linkcheck. Mimetypes shouldn't affect link checking
      - "mystnb.mime_priority"
      # From plotly. The output looks fine, so this seems safe to ignore.
      - "mystnb.unknown_mime_type"
      - "myst.xref_missing"
    linkcheck_ignore:
      # Seems to be too unreliable
      - 'https?://www\.crystallography\.net/'
      - 'https://people.sc.fsu.edu/~jburkardt/data/csv/csv.html'
    linkcheck_anchors_ignore_for_url:
      # These are redirected to a URL with a concrete version,
      # linkcheck cannot resolve anchors with this redirect.
      # This means that we have to manually check the anchors!
      - 'https?://docs\.easydiffraction\.org/lib/latest'
      - 'https?://easyscience\.github\.io/diffraction-lib/latest'

exclude_patterns:
  - 'article/article.md'
  - '.pixi/*'

only_build_toc_files: true
